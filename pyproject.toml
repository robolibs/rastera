[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "rastera"
version = "0.1.0"
description = "Python bindings for the rastera GeoTIFF / raster library"
requires-python = ">=3.9"

[tool.maturin]
features = ["python"]
module-name = "rastera"
